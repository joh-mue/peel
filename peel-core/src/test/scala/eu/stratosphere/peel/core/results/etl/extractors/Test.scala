package eu.stratosphere.peel.core.results.etl.extractors

object Test {

  def main(args: Array[String]): Unit = {
//    try {
//      val extractor = new JsonEventExtractorFactory
//      //val file = new File("/home/akunft/peel_old/results/kmeans.default/kmeans.spark.single-run.run01/logs/app-20150701110215-0003")
//
////      val manager = EventExtractionSystem().register("manager", ExtractorManager.props(), Seq(), false)
////      implicit val timeout = Timeout(10, TimeUnit.HOURS)
//
////      val run = CurrentRun("lala", 1)
//
//      //      val xs = for {
//      //        format <- extractor.format(null, file)
//      //        handler <- extractor.router(null, file)
//      //      } yield (format, handler)
//      //
//      //      for (x <- xs) {
//      //        val future = ask(manager, Start(file, x._1, x._2, run)).mapTo[End]
//      //        val retval = Await.result(future, timeout.duration)
//      //
//      //        println(retval.events.size)
//      //        println("READY")
//      //      }
//    } catch {
//      case e: Exception => e.printStackTrace()
//    }
  }
}
